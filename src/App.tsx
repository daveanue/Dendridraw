/**
 * Dendridraw — Semantic Mindmap Layer on Excalidraw
 *
 * App entry point: renders the Excalidraw-backed canvas.
 */
import Canvas from './components/Canvas';

export default function App() {
  return <Canvas />;
}
